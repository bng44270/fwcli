# fwcli
Auto-complete enabled CLI for iptables based on Bash  
  
### Usage:  
1. run ```configure```
2. Run ```make config```
3. Run ```make install```
4. Run ```sudo fwcli``` (requires sudo access)
